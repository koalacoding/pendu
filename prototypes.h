char f_read_char(); // Function like scanf without its buffer problem.

void f_starify_secret_word(char* word, char* starified_word); // Function to replace the secret word by a word of stars of the same length.

int f_is_letter_in_word(char* word, char letter); // Function to check if the letter the user chose is in the secret word.
